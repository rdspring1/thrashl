Use /debug for this task.

The check script is failing:

    ./check.sh

Investigate why. Rank at least 2 hypotheses before any experiment.
Run at least 2 experiments. For each experiment:
- Append a complete entry to debug-session.md BEFORE starting the next experiment.
- Each entry must include: Hypothesis, Exact change or command, Observed result, Interpretation.

When you have a leading hypothesis or hit a churn-guard trigger, emit a DEBUG NOTE and write it to save.md using /save.
Do not attempt to fix app.py — this is an investigation only.
