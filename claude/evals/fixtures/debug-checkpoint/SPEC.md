Use /debug for this task.

The check script is failing:

    ./check.sh

Investigate the failure. Maintain the experiment ledger in debug-session.md.
When the churn guard triggers, emit CHECKPOINT and write the final state to save.md using /save.
Do not attempt to fix app.py — this is a debugging investigation only.
