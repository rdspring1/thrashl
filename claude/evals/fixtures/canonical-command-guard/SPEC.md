Use /debug for this task.

The developer reports that running tests with:
    python test/foo.py
fails on every attempt. You can reproduce the failure with:
    ./run.sh

Investigate the failure. Check whether the repo defines a canonical test invocation.
Maintain the experiment ledger in debug-session.md.
When you have identified the root cause and blocker classification, write your findings
to save.md. Do not attempt to fix any code — this is a debugging and classification
investigation only.
