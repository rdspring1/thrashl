Use `/impl` for this task.

Edit `app.py`:
- change `message = "old"`
- to `message = "new"`

Then run:
`./validate.sh`

This is a successful, tiny, one-file, local implementation. If validation
passes, emit the normal implementation summary and do not write `save.md`.
